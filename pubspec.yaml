name: minimal_music_player
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.1 <4.0.0'

dependencies:
  audioplayers: ^5.2.1
  auto_size_text: ^3.0.0
  flutter:
    sdk: flutter
  intl: ^0.19.0
  marquee: ^2.2.3
  path_provider: ^2.1.2
  provider: ^6.1.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/music/
    - assets/covers/
